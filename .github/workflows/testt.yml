name: TDS Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 26ds1000002@ds.study.iitm.ac.in
        run: echo "Hello, world!"
